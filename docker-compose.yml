version: "3.8"

services:
  frontend:
    image: nginx:alpine
    container_name: property-app
    ports:
      - "8080:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
    restart: unless-stopped